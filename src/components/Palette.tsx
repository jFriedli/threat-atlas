import type { ElementType } from '../domain'; import { TypeIcon, typeName } from './Icons'; import { useStore } from '../state'
export function Palette(){const addNode=useStore(s=>s.addNode);return <aside className="palette"><div className="palette-heading">COMPONENTS</div>{(['external','process','store','boundary'] as ElementType[]).map((type,i)=><button key={type} draggable onDragStart={e=>{e.dataTransfer.setData('atlas/type',type);e.dataTransfer.effectAllowed='move'}} onClick={()=>addNode(type,{x:120+i*30,y:120+i*35})}><TypeIcon type={type}/><span>{typeName[type]}</span></button>)}<p>Drag onto the canvas or click to add.</p></aside>}
