import { test,expect } from '@playwright/test'
test('opens example and navigates core STRIDE workflow',async({page})=>{await page.goto('/');await expect(page).toHaveTitle(/Threat Atlas/);await expect(page.getByRole('heading',{name:'Threat models'})).toBeVisible();await page.getByRole('button',{name:/Online Banking Example/}).click();await expect(page.getByText('Web Application',{exact:true})).toBeVisible();await page.getByText('Web Application',{exact:true}).click();await page.getByRole('button',{name:/Threats 1/}).click();await expect(page.getByText('Credential Stuffing Against Customer Login')).toBeVisible();await page.getByRole('button',{name:/Review/}).click();await expect(page.getByRole('heading',{name:'Threat review'})).toBeVisible();await expect(page.getByText('Modification of API Requests')).toBeVisible()})
test('creates and persists a model and process',async({page})=>{await page.goto('/');await page.getByRole('button',{name:'Create model'}).click();await page.getByRole('button',{name:'Process',exact:true}).click();await expect(page.locator('.react-flow__node').getByText('New Process')).toBeVisible();await page.waitForTimeout(600);await page.reload();await expect(page.locator('.react-flow__node').getByText('New Process')).toBeVisible()})
test('contained nodes remain selectable after selecting a boundary and nodes resize',async({page})=>{await page.goto('/');await page.getByRole('button',{name:/Online Banking Example/}).click();await page.getByText('Application Network',{exact:true}).click();await page.getByText('Web Application',{exact:true}).click();await expect(page.getByRole('complementary').getByText('Web Application',{exact:true})).toBeVisible();const node=page.locator('.react-flow__node').filter({hasText:'Web Application'}),before=await node.boundingBox();const handle=node.locator('.atlas-resize-handle').last(),box=await handle.boundingBox();expect(box).not.toBeNull();await page.mouse.move(box!.x+5,box!.y+5);await page.mouse.down();await page.mouse.move(box!.x+55,box!.y+35,{steps:5});await page.mouse.up();const after=await node.boundingBox();expect(after!.width).toBeGreaterThan(before!.width)})
test('adds, routes, removes, and persists canvas waypoints',async({page})=>{await page.goto('/');await page.getByRole('button',{name:/Online Banking Example/}).click();await page.locator('.flow-label').first().evaluate(el=>{const r=el.getBoundingClientRect();el.dispatchEvent(new MouseEvent('dblclick',{bubbles:true,clientX:r.x+r.width/2,clientY:r.y+r.height/2}))});const handle=page.getByRole('button',{name:'Move waypoint 1'});await expect(handle).toBeVisible();await handle.click({button:'right'});await page.getByRole('button',{name:'Smooth'}).click();await page.waitForTimeout(500);await page.reload();await page.locator('.react-flow__edge').first().click({force:true});await expect(page.getByRole('button',{name:'Move waypoint 1'})).toBeVisible();await page.getByRole('button',{name:'Move waypoint 1'}).evaluate(el=>el.dispatchEvent(new MouseEvent('dblclick',{bubbles:true})));await expect(page.getByRole('button',{name:'Move waypoint 1'})).toHaveCount(0)})
